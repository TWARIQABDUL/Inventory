name: Keep Render Alive

on:
  schedule:
    - cron: "*/2 * * * *"   # every 10 minutes
  workflow_dispatch:         # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -s https://inventory-management-3ioh.onrender.com/api/products || true
